import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#64ffda',
    },
    secondary: {
      main: '#ef6c00',
    },
    error: {
      main: '#c2185b',
    },
  },
});

export default theme;
